Invalid WordPress sites configuration: site names in `sites` must have matching entry in `vault_sites`.

Sites without a matching vault entry:
{% for name in sites.keys() | difference(vault_sites.keys()) %}
* `{{ name }}`
{% endfor %}

Update `group_vars/{{ env }}/vault.yml` to continue.

Docs: https://roots.io/trellis/docs/wordpress-sites/#passwordssecrets
