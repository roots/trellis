{% for key, value in project.env.iteritems() %}
{{ key | upper }}="{{ value }}"
{% endfor %}
{% for key, value in vault_wordpress_sites[site].env.iteritems() %}
{{ key | upper }}="{{ value }}"
{% endfor %}
