# {{ ansible_managed }}

# https://support.cloudflare.com/hc/en-us/articles/200170706

{% for cloudflare_ip in ( cloudflare_ips_v4.content + '\n' + cloudflare_ips_v6.content ).split( '\n' ) | ipaddr %}
set_real_ip_from {{ cloudflare_ip }};
{% endfor %}

real_ip_header CF-Connecting-IP;
